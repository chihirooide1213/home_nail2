
<div class="container">
	<h2>マイページ</h2>
		<% @nails.each do |nail| %>
		 <%= nail.name %>
		 <%= attachment_image_tag nail,:image, :fill, 50, 50 ,format: "jpeg" %>
		 <%= nail.price %>
		 <%= nail.introduction %>
		<% end %>

	<%= link_to "新規投稿", new_nail_path %>
</div>